import { ClipboardCheck, ListTodo } from 'lucide-react'
import { Card, Page } from '../../../shared/components'
export function WorkflowPage() { return <Page eyebrow="Operations" title="Workflow inbox" description="Centralized, auditable work states, approvals, and assignments. Existing module rules remain authoritative."><div className="grid gap-4 sm:grid-cols-2"><Card className="p-5"><ListTodo className="text-emerald-700" size={22} /><h2 className="mt-4 font-semibold">My tasks</h2><p className="mt-1 text-sm text-slate-500">Tasks and approvals will consume workflow instances rather than separate module queues.</p></Card><Card className="p-5"><ClipboardCheck className="text-emerald-700" size={22} /><h2 className="mt-4 font-semibold">Audit timeline</h2><p className="mt-1 text-sm text-slate-500">Transitions use immutable history entries and idempotency keys.</p></Card></div></Page> }
