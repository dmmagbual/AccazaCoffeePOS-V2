import { BarChart3, Gauge } from 'lucide-react'
import { Card, Page } from '../../../shared/components'
export function AnalyticsPage() { return <Page eyebrow="Business intelligence" title="Analytics" description="Registered metrics, KPIs, and reusable analytical providers. Current views remain branch-aware and simple."><div className="grid gap-4 sm:grid-cols-2"><Card className="p-5"><BarChart3 className="text-emerald-700" size={22} /><h2 className="mt-4 font-semibold">Metric catalog</h2><p className="mt-1 text-sm text-slate-500">Versioned metrics are calculated once by providers and reused by future widgets and reports.</p></Card><Card className="p-5"><Gauge className="text-emerald-700" size={22} /><h2 className="mt-4 font-semibold">KPI scorecards</h2><p className="mt-1 text-sm text-slate-500">Target evaluation is deterministic, traceable, and safe with no-data handling.</p></Card></div></Page> }
