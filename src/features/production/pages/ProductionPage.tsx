import { Factory, PackageCheck, TrendingDown } from 'lucide-react'
import { Card, Page } from '../../../shared/components'
export function ProductionPage() { return <Page eyebrow="Manufacturing" title="Production" description="Transform approved raw and semi-finished inputs into traceable finished batches."><div className="grid gap-4 sm:grid-cols-3"><Metric icon={Factory} label="Pending production" /><Metric icon={PackageCheck} label="Completed today" /><Metric icon={TrendingDown} label="Yield variance" /></div><Card className="mt-5 p-5"><h2 className="font-semibold">Production workflow</h2><p className="mt-2 text-sm text-slate-600">Approve a versioned production recipe, issue inputs through the shared FIFO consumption engine, record actual yield and waste, then complete production to create an expiring finished batch with calculated unit cost.</p></Card></Page> }
function Metric({ icon: Icon, label }: { icon: typeof Factory; label: string }) { return <Card className="p-5"><Icon className="text-emerald-700" size={20} /><p className="mt-4 text-sm text-slate-500">{label}</p><strong className="text-3xl">0</strong></Card> }
