import { Building2, CheckCircle2, MapPin, Warehouse } from 'lucide-react'
import { Card, Page } from '../../../shared/components'
export function PlatformConfigurationPage() { return <Page eyebrow="Settings" title="Platform configuration" description="Simple storage is active for the current branch. Advanced warehouse capabilities stay unavailable until their workflows are released."><div className="grid gap-4 md:grid-cols-2"><Card className="p-5"><Building2 className="text-emerald-700" size={22} /><h2 className="mt-4 font-semibold">Inventory operating mode</h2><p className="mt-1 text-sm text-slate-500">Simple storage · default mode</p><p className="mt-4 flex items-center gap-2 text-sm text-emerald-700"><CheckCircle2 size={16} />No warehouse complexity required.</p></Card><Card className="p-5"><MapPin className="text-emerald-700" size={22} /><h2 className="mt-4 font-semibold">Default branch location</h2><p className="mt-1 text-sm text-slate-500">Main Storage is provisioned per branch for receiving and production output.</p></Card><Card className="p-5"><CheckCircle2 className="text-emerald-700" size={22} /><h2 className="mt-4 font-semibold">Active capabilities</h2><p className="mt-1 text-sm text-slate-500">Location tracking, batch status, and same-branch location transfers.</p></Card><Card className="p-5"><Warehouse className="text-slate-400" size={22} /><h2 className="mt-4 font-semibold">Advanced warehouse</h2><p className="mt-1 text-sm text-slate-500">Planned. Zones, bins, directed put-away, and pick lists are intentionally unavailable.</p></Card></div></Page> }
