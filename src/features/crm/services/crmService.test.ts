import { describe, expect, it } from 'vitest'
import { applyLoyaltyTransaction, findCustomerDuplicates, selectPromotion, validateCoupon } from '.'
import type { Customer, LoyaltyAccount, Promotion } from '../domain'
const now = new Date('2026-07-31T00:00:00.000Z')
const customer: Customer = { id: 'c', customerNumber: 'C-1', organizationId: 'org', homeBranchId: null, preferredBranchId: null, membershipId: null, firstName: 'Ana', middleName: '', lastName: 'Cruz', displayName: 'Ana Cruz', mobileNumber: '0917', email: 'ana@example.com', birthDate: now, marketingConsent: false, smsConsent: false, emailConsent: false, status: 'ACTIVE', mergedIntoCustomerId: null, createdAt: now, updatedAt: now, createdBy: 'u', updatedBy: 'u' }
describe('CRM service', () => { it('finds duplicates and preserves immutable loyalty balances', () => { expect(findCustomerDuplicates(customer, [customer])).toHaveLength(1); const account: LoyaltyAccount = { id: 'l', organizationId: 'org', customerId: 'c', availablePoints: 10, lifetimePoints: 10, updatedAt: now }; expect(applyLoyaltyTransaction(account, { customerId: 'c', saleId: 's', branchId: 'b', type: 'REDEEM', points: -5, reason: 'reward', performedBy: 'u', performedAt: now }, now).account.availablePoints).toBe(5) }); it('prioritizes promotions and blocks exhausted coupons', () => { const promotion: Promotion = { id: 'p', organizationId: 'org', name: 'Promo', type: 'PERCENTAGE', priority: 2, stackable: false, startAt: new Date('2026-01-01'), endAt: new Date('2026-12-31'), minimumSpend: 100, maximumDiscount: null, active: true }; expect(selectPromotion([promotion], 200, now)?.id).toBe('p'); expect(() => validateCoupon({ id: 'c', organizationId: 'org', code: 'X', promotionId: 'p', active: true, singleUse: true, usedCount: 1, usageLimit: null, expiresAt: null }, now)).toThrow('eligible') }) })
