import { ArrowLeftRight, Truck } from 'lucide-react'
import { Card, Page } from '../../../shared/components'
export function InterBranchTransfersPage() { return <Page eyebrow="Logistics" title="Inter-branch transfers" description="Move approved, traceable stock between authorized company-owned branches."><div className="grid gap-4 sm:grid-cols-2"><Card className="p-5"><ArrowLeftRight className="text-emerald-700" size={22} /><h2 className="mt-4 font-semibold">Transfer lifecycle</h2><p className="mt-1 text-sm text-slate-500">Draft through dispatch, in transit, receiving, and completion with no destructive edits.</p></Card><Card className="p-5"><Truck className="text-emerald-700" size={22} /><h2 className="mt-4 font-semibold">In-transit stock</h2><p className="mt-1 text-sm text-slate-500">Source batches retain cost, expiry, and supplier or production lineage until accepted at destination.</p></Card></div></Page> }
