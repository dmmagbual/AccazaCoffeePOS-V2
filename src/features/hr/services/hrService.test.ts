import { describe, expect, it } from 'vitest'
import { findPersonDuplicates, summarizeAttendance, validateBranchAssignment } from '.'
import type { AttendanceEvent, EmployeeBranchAssignment, Person } from '../domain'
const now = new Date('2026-08-01T08:00:00.000Z')
const person: Person = { id: 'p', personNumber: 'PER-1', organizationId: 'org', firstName: 'Ana', middleName: null, lastName: 'Cruz', displayName: 'Ana Cruz', birthDate: now, mobileNumber: '0917', email: 'ana@example.com', status: 'ACTIVE', tags: [], createdAt: now, createdBy: 'u', updatedAt: now, updatedBy: 'u' }
describe('HR services', () => { it('detects people duplicates and protects primary assignments', () => { expect(findPersonDuplicates(person, [person])).toHaveLength(1); const assignment: EmployeeBranchAssignment = { id: 'a', organizationId: 'org', employeeId: 'e', branchId: 'b', positionId: 'cashier', assignmentType: 'PRIMARY', effectiveFrom: now, effectiveTo: null, primaryAssignment: true, status: 'ACTIVE', createdAt: now, createdBy: 'u' }; expect(() => validateBranchAssignment({ ...assignment, id: 'b' }, [assignment])).toThrow('one active') }); it('summarizes attendance without payroll calculations', () => { const events: AttendanceEvent[] = [{ id: 'in', organizationId: 'org', branchId: 'b', employeeId: 'e', eventType: 'CLOCK_IN', occurredAt: now, source: 'POS', shiftId: null, recordedBy: 'e', correctionOfEventId: null }, { id: 'out', organizationId: 'org', branchId: 'b', employeeId: 'e', eventType: 'CLOCK_OUT', occurredAt: new Date('2026-08-01T16:00:00.000Z'), source: 'POS', shiftId: null, recordedBy: 'e', correctionOfEventId: null }]; expect(summarizeAttendance(events, 'e', 'b')).toMatchObject({ actualMinutes: 480, status: 'PRESENT' }) }) })
