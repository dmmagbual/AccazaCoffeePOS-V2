import type { FranchiseAgreement, FranchiseOrganization, RoyaltyCalculation, RoyaltyRule } from '../domain'
export function validateFranchiseAgreement(agreement: FranchiseAgreement, organization: FranchiseOrganization): void { if (agreement.franchiseOrganizationId !== organization.id || agreement.expirationDate <= agreement.effectiveDate || agreement.allowedBranchCount < 1) throw new Error('Franchise agreement is invalid.'); if (agreement.status === 'ACTIVE' && agreement.signedAt === null) throw new Error('An active agreement must be signed.') }
export function calculateRoyalty(rule: RoyaltyRule, salesBasisAmount: number, exclusions: number, context: Pick<RoyaltyCalculation, 'franchiseOrganizationId' | 'branchId' | 'agreementId' | 'periodStart' | 'periodEnd'>, now = new Date()): RoyaltyCalculation { const basis = Math.max(0, salesBasisAmount - exclusions); let royaltyAmount = rule.basis === 'FIXED_AMOUNT' ? rule.fixedAmount ?? 0 : basis * rule.rate; if (rule.minimumAmount !== null) royaltyAmount = Math.max(royaltyAmount, rule.minimumAmount); if (rule.maximumAmount !== null) royaltyAmount = Math.min(royaltyAmount, rule.maximumAmount); return { ...context, salesBasisAmount, exclusions, royaltyAmount: Math.round(royaltyAmount * 100) / 100, calculationVersion: 1, status: 'CALCULATED', calculatedAt: now } }
