import { PackagePlus, Truck, Users } from 'lucide-react'
import { Card, Page } from '../../../shared/components'
const cards = [{ title: 'Open purchase orders', value: '0', icon: PackagePlus }, { title: 'Pending deliveries', value: '0', icon: Truck }, { title: 'Active suppliers', value: '0', icon: Users }]
export function ProcurementPage() { return <Page eyebrow="Procurement & inventory" title="Procurement dashboard" description="Manage suppliers, purchases, receiving, and FIFO inventory batches."><div className="grid gap-4 sm:grid-cols-3">{cards.map(({ title, value, icon: Icon }) => <Card key={title} className="p-5"><Icon size={20} className="text-emerald-700" /><p className="mt-4 text-sm text-slate-500">{title}</p><strong className="mt-1 block text-3xl">{value}</strong></Card>)}</div><Card className="mt-5 p-5"><h2 className="font-semibold">Receiving workflow</h2><p className="mt-2 text-sm text-slate-600">Receive approved purchase orders, direct purchases, or online purchases. Accepted quantities create immutable FIFO inventory batches with cost, supplier, branch, and expiry details.</p><div className="mt-4 grid gap-3 sm:grid-cols-3"><Step title="1. Source purchase" body="Purchase Order, direct, Shopee, Lazada, or marketplace." /><Step title="2. Receive goods" body="Record accepted, rejected, damaged, invoice, and batch data." /><Step title="3. Value FIFO" body="Use oldest available batches for future recipe consumption." /></div></Card></Page> }
function Step({ title, body }: { title: string; body: string }) { return <div className="rounded-lg bg-slate-50 p-4"><strong className="text-sm">{title}</strong><p className="mt-2 text-xs text-slate-500">{body}</p></div> }
