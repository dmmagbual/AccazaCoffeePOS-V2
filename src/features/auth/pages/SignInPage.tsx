import { zodResolver } from '@hookform/resolvers/zod'
import { useForm } from 'react-hook-form'
import { z } from 'zod'
import { Button } from '../../../shared/components'
const schema = z.object({ email: z.string().email('Enter a valid email address'), password: z.string().min(8, 'Password must be at least 8 characters') })
type SignInForm = z.infer<typeof schema>
export function SignInPage() { const { register, handleSubmit, formState: { errors } } = useForm<SignInForm>({ resolver: zodResolver(schema) }); return <main className="grid min-h-screen place-items-center bg-slate-950 p-5"><form className="w-full max-w-sm rounded-2xl bg-white p-8 shadow-xl" onSubmit={handleSubmit(() => undefined)}><p className="text-xs font-bold uppercase tracking-widest text-emerald-700">Accaza Business Platform</p><h1 className="mt-3 font-serif text-3xl">Welcome back</h1><p className="mt-2 text-sm text-slate-500">Sign in to access your workspace.</p><label className="mt-7 block text-sm font-medium">Email<input className="mt-1.5 w-full rounded-lg border border-slate-300 px-3 py-2.5 outline-none focus:border-emerald-600" type="email" {...register('email')} /></label>{errors.email && <p className="mt-1 text-xs text-rose-600">{errors.email.message}</p>}<label className="mt-4 block text-sm font-medium">Password<input className="mt-1.5 w-full rounded-lg border border-slate-300 px-3 py-2.5 outline-none focus:border-emerald-600" type="password" {...register('password')} /></label>{errors.password && <p className="mt-1 text-xs text-rose-600">{errors.password.message}</p>}<Button className="mt-6 w-full" type="submit">Sign in</Button></form></main> }
