export interface RecipeIngredientRequirement { ingredientId: string; ingredientNameSnapshot: string; baseUnitQuantity: number; unitId: string; optional: boolean }
export interface RecipeDocument { id: string; organizationId: string; recipeNumber: string; productId: string; productVariationId?: string | null; recipeName: string; published: boolean; recipeStatus: string; effectiveFrom?: Date | null; effectiveTo?: Date | null; sourceOrganizationId?: string | null; protectedHeadOfficeRecipe?: boolean; activeVersionId?: string | null }
export interface RecipeVersionDocument { id: string; organizationId: string; recipeId: string; businessVersion: string; status: string; effectiveFrom?: Date | null; effectiveTo?: Date | null; ingredients: readonly RecipeIngredientRequirement[]; packagingRequirements?: readonly RecipeIngredientRequirement[]; semiFinishedRequirements?: readonly RecipeIngredientRequirement[]; yieldQuantity: number; yieldUnitId: string }
export interface RecipeSnapshot { recipeId: string; recipeNumber: string; recipeVersionId: string; businessVersion: string; recipeName: string; ingredientRequirements: readonly RecipeIngredientRequirement[]; packagingRequirements: readonly RecipeIngredientRequirement[]; yieldQuantity: number; yieldUnitId: string; sourceOrganizationId: string; protected: boolean }
