export interface TaxProfileDocument { id: string; organizationId: string; code: string; name: string; taxType: string; active: boolean; priority: number; taxPayableAccountId?: string | null; branchIds?: readonly string[] | null; defaultForOrganization?: boolean; defaultForBranchIds?: readonly string[] }
export interface TaxVersionDocument { id: string; organizationId: string; taxProfileId: string; rate: string; calculationMode: 'TAX_INCLUSIVE' | 'TAX_EXCLUSIVE' | 'NOT_APPLICABLE'; roundingMethod: string; roundingPrecision: number; active: boolean; effectiveFrom: Date; effectiveTo?: Date | null }
export interface TaxAssignmentDocument { id: string; organizationId: string; scope: 'BRANCH' | 'CATEGORY' | 'PRODUCT' | 'VARIATION' | 'OPTION'; scopeId: string; taxProfileId: string; active: boolean; effectiveFrom?: Date | null; effectiveTo?: Date | null }
export interface TaxSnapshot { taxProfileId: string | null; taxConfigurationVersionId: string | null; taxCode: string; taxName: string; taxType: string; rateApplied: string; calculationMode: TaxVersionDocument['calculationMode']; roundingMethod: string; roundingPrecision: number; taxPayableAccountId?: string }
