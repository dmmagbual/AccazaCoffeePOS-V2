export interface PaymentMethodDocument { id: string; organizationId: string; code: string; name: string; active: boolean; branchIds?: readonly string[] | null; currencyCode?: string | null; financialAccountId?: string | null; settlementCategory: string; effectiveFrom?: Date | null; effectiveTo?: Date | null; requiresTransactionReference?: boolean; transactionReferencePattern?: string | null; transactionReferenceMinLength?: number | null; transactionReferenceMaxLength?: number | null; requiresTenderedAmount?: boolean; requiresFinancialAccount?: boolean }
export interface PaymentMethodSnapshot { paymentMethodId: string; code: string; name: string; settlementCategory: string; currencyCode?: string; financialAccountId?: string }
export interface ValidatedTender { snapshot: PaymentMethodSnapshot; amount: number; tenderedAmount: number | null; transactionReference: string | null; isCash: boolean }
