# Money and Quantity Standard

Keep currency code with money values. Centralize rounding at service boundaries and preserve six-decimal inventory quantity precision where the consumption engine requires it. Do not use UI calculations as financial truth.
