# Date and Time Standard

Persist timestamps in UTC through Firebase timestamps. Date-only business fields must be explicitly documented. Business reporting and schedule evaluation must use organization or branch timezone; avoid UTC string grouping for operational reports.
