# Code Quality Standard

Use strict TypeScript without `any`, expose feature APIs through barrel exports, retain business calculations in services/application layers, and keep React components presentational. Validate external input with Zod before persistence. New service behavior requires focused tests, including failure paths.
