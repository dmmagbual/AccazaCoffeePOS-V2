# Firebase Emulator Testing

Run `npm run emulators:test` locally after installing Firebase CLI and the Firestore emulator runtime. It starts Firestore, Functions, and Auth emulators from `firebase.json`; no production credentials are required.
