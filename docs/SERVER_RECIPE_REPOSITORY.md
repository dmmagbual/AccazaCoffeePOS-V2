# Server Recipe Repository

Published recipes are resolved only from `recipes` and their `versions` subcollection. Exactly one effective published version must apply to a product or variation. Protected Head Office recipes reject branch overrides.
