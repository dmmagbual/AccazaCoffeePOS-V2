# Reference Data

Reference data contains rarely changing controlled values such as status, priority, payment method, temperature class, approval status, and reason code. It is deliberately separate from frequently edited business masters.
