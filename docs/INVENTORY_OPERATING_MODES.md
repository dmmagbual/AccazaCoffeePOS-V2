# Inventory Operating Modes

`SIMPLE_STORAGE` is the current default. It uses a branch default location and optional named areas such as bar, chiller, freezer, receiving, and production. `ADVANCED_WAREHOUSE` is modeled as a planned capability and is deliberately not exposed.
