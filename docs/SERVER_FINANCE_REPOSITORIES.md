# Server Finance Repositories

P4-002C.0 provides Admin SDK-only account, accounting-period, posting-configuration, and journal repositories in `functions/src/finance`. Account mappings are configuration driven, organization and branch scoped, and are never accepted from a client sale request. This milestone prepares, but does not yet post, trusted sale journals.
