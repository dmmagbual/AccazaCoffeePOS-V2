# Firebase Functions Foundation

The `functions/` project is a separate Node 20 TypeScript runtime. It uses Firebase Admin only; browser React, Zustand, Vite modules, and client Firebase imports are prohibited. No sale callable handler is deployed by this foundation.
