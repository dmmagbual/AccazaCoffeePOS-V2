# Server Branch Authorization

Authorization resolves `organizations`, `branches`, `employees`, `employeeBranchAssignments`, and `shifts`. Claims establish identity only; organization, branch, employee assignment, permissions, and open shift are validated against trusted records.
