# Disaster Recovery

For a single-store pilot, stop affected writes, preserve the last known-good
export, restore only into an isolated recovery project, reconcile immutable ledger
counts and inventory balances, and obtain finance/operations sign-off before
resuming. Future multi-branch recovery must validate each store and franchise
organization independently. Do not claim a recovery completed until rule access,
totals, and attachment availability are verified.
