# Persistent Catalog Server Infrastructure

P4-001A.1 adds server-only Admin SDK repositories and a bounded branch-scoped catalog read model. Required composite indexes are recorded in `firestore.indexes.json`. P4-002 remains open: a secure callable, durable trusted-sale persistence, shift totals, finance, loyalty, and duplicate-effect integration are still required.
