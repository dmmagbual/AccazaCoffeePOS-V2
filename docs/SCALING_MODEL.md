# Scaling Model

Stage 1 — one store uses simple storage and `Main Storage` automatically.

Stage 2 — each company-owned branch receives its own default location and isolated inventory scope.

Stage 3 — franchise organizations use separate organization ownership and permission-controlled head-office access.

Stage 4 — enterprise deployments may enable modeled warehouse hierarchy and related capabilities after those workflows are implemented.
