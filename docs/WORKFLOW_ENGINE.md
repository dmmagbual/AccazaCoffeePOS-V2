# Workflow Engine

The workflow engine governs state, approval, task, and history concerns while business modules retain their own invariants. Instances retain definition versions; every transition is authorized and idempotent.
